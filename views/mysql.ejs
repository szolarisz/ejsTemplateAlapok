<!DOCTYPE html>
<html lang="hu">

<head>
    <meta charset="UTF-8">

    <title>EJS Template ismerkedés - MySQL</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

</head>

<body style="background-color:azure; color:cadetblue">
    <h1> EJS Template példakód</h1>
    <h2>Telefonhívások listája MySQL adatbázisból</h2>
    <a href="/"><button type="button" class="btn btn-primary">dolgozz JSON állományból</button></a>
    <% for( let hivas of hivasok) {
        let hossz = hivas.vegeOra * 3600 + hivas.vegePerc * 60 + hivas.vegeMp
        hossz -= ( hivas.kezdOra * 3600 + hivas.kezdPerc * 60 + hivas.kezdMp) %>
    <div style="margin-top: 5px; width: 222px; background-color: <%= hossz > 180 ? 'crimson' : 'beige' %>;" >
        <%= hivas.kezdOra %>:
        <%= hivas.kezdPerc %>:
        <%= hivas.kezdMp %> -
        <%= hivas.vegeOra %>:
        <%= hivas.vegePerc %>:
        <%= hivas.vegeMp %> (<%= hossz %>sec)
    </div>
    <% } %>

</body>

</html>